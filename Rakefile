# frozen_string_literal: true

require "rake/testtask"
require "rake/extensiontask"

Rake::ExtensionTask.new("magnus_multi_build") do |c|
  c.name = "magnus_multi_build"
  c.lib_dir = "lib/magnus_multi_build"
end